<?xml version="1.0" encoding="utf-8"?>
<!-- 원모양의 시크바 컨트롤러 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false"  >
    <!-- 배경 -->
    <solid
        android:color="@color/drawing_penalpha_seekbar_tumb_solid"/>
    <!-- 테두리 -->
    <stroke
        android:width="2dp"
        android:color="@color/drawing_penalpha_seekbar_tumb_storke" />
    <!-- 크기 -->
    <size
        android:width="@dimen/seekbar_tumb_width"
        android:height="@dimen/seekbar_tumb_width"/>
</shape>